const EXPECTED = 'string';
const ACTUAL = 'string';

it('test 1: get old null case', async () => {
  expect(EXPECTED).toBe(ACTUAL);
});

it('test 2: get old non-null case', async () => {
  expect(EXPECTED).toBe(ACTUAL);
});

it('test 3: set old', async () => {
  expect(EXPECTED).toBe(ACTUAL);
});

it('test 4: get bullet', async () => {
  expect(EXPECTED).toBe(ACTUAL);
});

it('test 5: set category list', async () => {
  expect(EXPECTED).toBe(ACTUAL);
});

it('test 6: set length violate', async () => {
  expect(EXPECTED).toBe(ACTUAL);
});