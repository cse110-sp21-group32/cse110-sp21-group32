const FAIL_STR = 'Not yet implemented';

describe('Adding categories', () => {
  it('test 1: Add category opens up a dialog box', async () => {
    
  });

  it('test 2: Newly created category shows up in category manager', async () => {
    
  });
});

describe('Display categories', () => {
  it('test 1: Name of category correctly displayed', async () => {
    
  });
  
  it('test 2: Category displays proper color', async () => {
    fail(FAIL_STR);
  });
});

describe('Selecting categories', () => {
  it('test 1: Selected categories show in Focus', async () => {
    fail(FAIL_STR);
  });

  it('test 2: Select all button functions appropriately', async () => {
    fail(FAIL_STR);
  });

  it('test 3: Deselect all button functions appropriately', async () => {
    fail(FAIL_STR);
  })
});

describe('Deleting categories', () => {
  it('test 1: Deleted category disappears from right side list', async () => {
    fail(FAIL_STR);
  });
  
  it('test 2: Bullets of a deleted category respond appropriately', async () => {
    fail(FAIL_STR);
  });
});

describe('Editing categories', () => {
  it('test 1: Category with edited title changes title', async() => {
    fail(FAIL_STR);
  });
  
  it('test 2: Category with edited color changes color', async() => {
    fail(FAIL_STR);
  });
});